{
  "name": "kotori2",
  "version": "1.0.3",
  "description": "Bot de Discord con soporte para reproducción de audio",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.14.0",
    "npm": ">=10.2.4"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "format": "node ./node_modules/prettier/bin-prettier.js --write \"src/**/*.ts\"",
    "check": "npm run lint && npm run build && npm run test",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "release:patch": "npm version patch && git push --follow-tags && npm run deploy:docs",
    "release:minor": "npm version minor && git push --follow-tags && npm run deploy:docs",
    "release:major": "npm version major && git push --follow-tags && npm run deploy:docs",
    "deploy:docs": "npm run test:coverage || true && gh-pages -d docs"
  },
  "notes": {
    "check": "El formato se ha eliminado temporalmente de 'check' debido a problemas de permisos. Para resolverlo: 1) Instalar prettier globalmente (npm install -g prettier@3.2.5), 2) Ejecutar: chmod +x ./node_modules/prettier/bin-prettier.js, 3) Cambiar el script por: npx prettier --write \"src/**/*.ts\""
  },
  "keywords": [
    "discord",
    "bot",
    "typescript",
    "audio"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@discordjs/voice": "^0.16.1",
    "chalk": "4.1.2",
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.11.24",
    "@typescript-eslint/eslint-plugin": "^7.1.0",
    "@typescript-eslint/parser": "^7.1.0",
    "eslint": "^8.57.0",
    "gh-pages": "^6.3.0",
    "jest": "^29.7.0",
    "prettier": "3.2.5",
    "ts-jest": "^29.2.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "5.5.4"
  },
  "versionPolicy": {
    "type": "semver",
    "majorVersions": "Cambios que rompen la compatibilidad (breaking changes)",
    "minorVersions": "Nuevas funcionalidades que mantienen compatibilidad",
    "patchVersions": "Correcciones de errores que mantienen compatibilidad"
  }
}
